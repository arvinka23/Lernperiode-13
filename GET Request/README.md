# GET Request

Einfaches Rust-Programm für einen HTTP GET-Request (z. B. für Lernperiode 13).

- Verwendet `reqwest` (blocking) und `anyhow` für Fehlerbehandlung
- Ruft httpbin.org/get auf und gibt Status, Headers und Body aus

## Ausführen

```bash
cargo run
```
